# Core data + ML
pylangacq>=0.23.0
pandas>=2.0
numpy>=1.24
scipy>=1.11
scikit-learn==1.9.0
matplotlib>=3.7
seaborn>=0.12
torch>=2.0
markdown2>=2.4
weasyprint>=61.0
reportlab>=4.0
fastapi>=0.115.0
python-multipart>=0.0.9
uvicorn[standard]>=0.30.0
psycopg[binary]>=3.2.0
supabase>=2.0.0

# Audio pipeline (Whisper -> diarization -> CHAT)
faster-whisper>=1.0.0        # Fast ASR (4x faster than openai-whisper)
librosa>=0.10.0              # Audio loading + analysis (pitch, silence)
soundfile>=0.12.0            # Audio I/O
jiwer>=3.0.0                 # WER evaluation against TalkBank ground truth
speechbrain>=1.0.0           # ECAPA-TDNN speaker embeddings (no HF token needed)
torchaudio>=2.0              # Audio tensor backend for speechbrain
# silero-VAD is fetched via torch.hub at runtime (no extra pip dep needed)

# Optional: pyannote.audio for speaker diarization (needs HF token)
# Uncomment if you have a HuggingFace token set in HF_TOKEN env var:
# pyannote.audio>=3.1.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

openai>=1.0.0
pythainlp>=4.0.0
rich>=13.0.0
