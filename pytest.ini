[pytest]
pythonpath = . apps/api src
markers =
    slow: mark test as slow to run.
    audio: mark test as requiring heavy audio dependencies.
    assessment_postgres: tests requiring the isolated assessment PostgreSQL database.

